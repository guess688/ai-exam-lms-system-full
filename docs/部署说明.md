# 部署说明

## 部署架构

```text
浏览器
  |
  | HTTP
  v
Vue 前端 / Nginx
  |
  | /api 反向代理
  v
Spring Boot 后端
  |
  +-- MySQL
  +-- Redis
  +-- MinIO / OSS
  +-- AI Provider 或 mock AI
```

## 后端配置

配置文件：

```text
backend/src/main/resources/application-dev.yml
```

重点配置：

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db_exam?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: your_mysql_password
  redis:
    host: 127.0.0.1
    port: 6379

ai:
  provider: ${AI_PROVIDER:mock}
  apiKey: ${AI_API_KEY:}
  enabled: ${AI_ENABLED:false}
  mockEnabled: ${AI_MOCK_ENABLED:true}
```

生产环境建议：

- 数据库账号使用最小权限。
- Redis 设置访问密码并限制内网访问。
- MinIO/OSS Key 使用环境变量或部署平台密钥管理。
- JWT secret、AI API Key 不提交到仓库。

## 数据库部署

1. 创建数据库：

```sql
CREATE DATABASE IF NOT EXISTS db_exam DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行 SQL：

```bash
mysql -uroot -p db_exam < backend/sql/db_exam.sql
mysql -uroot -p db_exam < backend/sql/20260628_course_content.sql
mysql -uroot -p db_exam < backend/sql/20260629_question_course_difficulty.sql
mysql -uroot -p db_exam < backend/sql/20260629_online_course.sql
mysql -uroot -p db_exam < backend/sql/20260629_learning_task.sql
mysql -uroot -p db_exam < backend/sql/20260630_ai_infrastructure.sql
mysql -uroot -p db_exam < backend/sql/20260630_ai_report.sql
mysql -uroot -p db_exam < backend/sql/20260630_ai_generated_question.sql
```

客户演示环境再执行：

```bash
mysql -uroot -p db_exam < backend/sql/20260630_demo_data.sql
```

## 后端启动

开发模式：

```bash
cd backend
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

打包部署：

```bash
cd backend
mvn clean package -DskipTests
java -jar target/*.jar --spring.profiles.active=dev
```

## 前端启动

开发模式：

```bash
cd frontend
npm install --legacy-peer-deps
npm run dev
```

生产构建：

```bash
cd frontend
npm install --legacy-peer-deps
npm run build:prod
```

构建产物通常在：

```text
frontend/dist/
```

## Nginx 示例

```nginx
server {
    listen 80;
    server_name example.com;

    root /var/www/ai-exam-lms-system/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## 验证步骤

1. 访问前端首页。
2. 使用 `admin / 123456` 登录，查看用户和班级。
3. 使用 `teacher / 123456` 登录，查看课程、题库、考试、学习任务。
4. 使用 `student / 123456` 登录，查看考试、课程、学习任务和 AI 个人报告。
5. 调用任意 AI 功能，确认 mock 模式下可返回内容。

## 常见部署问题

| 问题 | 排查方向 |
| --- | --- |
| 登录失败 | 检查后端是否启动、Redis 是否可用、数据库账号密码是否正确 |
| 前端请求 404 | 检查代理或 Nginx `/api` 转发配置 |
| SQL 执行失败 | 确认已按顺序执行迁移脚本 |
| AI 返回失败 | mock 模式先确认 `AI_MOCK_ENABLED=true`；真实模式检查 API Key 和 baseUrl |
| 视频无法播放 | 确认视频 URL 可访问，浏览器允许跨域或静态服务已配置 |
