# AI Learning Adapted Assets

Adapted from `references/studyield` concepts:

- AI quiz JSON generation
- adaptive practice difficulty selection
- learning path step structure
- teach-back style evaluation dimensions
- analytics fields such as accuracy, weak topics, streak/activity signals

These files are reference assets only. They are also synchronized into `backend/src/main/resources/prompts` where the main `AiService` can load them.
